defaults: &defaults
  host: localhost
  # slaves:
  #   - host: slave1.local
  #     port: 27018
  #   - host: slave2.local
  #     port: 27019

development:
  <<: *defaults
  database: audit_man_development

test:
  <<: *defaults
  database: audit_man_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: audit_man_development
